apiVersion: v1
kind: Service
metadata:
  name: numberone-api-service
  namespace: numberone
spec:
  type: ClusterIP
  selector:
    app: numberone-api
  ports:
    - name: http
      port: 80
      targetPort: http
